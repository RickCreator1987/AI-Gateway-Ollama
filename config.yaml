gateway:
  host: "0.0.0.0"
  port: 8080
  log_level: "info"

ollama:
  servers:
    - "http://localhost:11434"
  health_check_interval: 30

rate_limit:
  enabled: true
  max_requests: 100
  time_window: 60

auth:
  api_keys: []
  header_name: "Authorization"
